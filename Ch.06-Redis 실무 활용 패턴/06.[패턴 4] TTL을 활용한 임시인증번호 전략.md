# [패턴 4] TTL을 활용한 임시인증번호 (OTP)

> 강의노트 Ch.06 — Redis 실무 활용 패턴 / [패턴 4] TTL을 활용한 임시인증번호 전략

---

## 실습 코드

```python
import random
import hashlib
from contextlib import asynccontextmanager
from fastapi import FastAPI, HTTPException, Request
from pydantic import BaseModel
import redis.asyncio as redis

@asynccontextmanager
async def lifespan(app: FastAPI):
    app.state.redis = redis.from_url("redis://localhost:6379/0", decode_responses=True)
    yield
    await app.state.redis.aclose()

app = FastAPI(lifespan=lifespan)

# 인증번호 유효 시간 (300초 = 5분)
AUTH_TIMEOUT = 300

class SendCodeRequest(BaseModel):
    phone: str

class VerifyCodeRequest(BaseModel):
    phone: str
    input_code: str

@app.post("/auth/send")
async def send_verification_code(req_data: SendCodeRequest, request: Request):
    rd = request.app.state.redis

    # 1. 6자리 난수 생성
    code = str(random.randint(100000, 999999))

    # 2. 전화번호(PII)를 SHA-256으로 해싱하여 캐시 키 생성
    hashed_phone = hashlib.sha256(req_data.phone.encode()).hexdigest()
    cache_key = f"auth:code:{hashed_phone}"

    # 3. Redis에 저장 (TTL 300초)
    await rd.set(cache_key, code, ex=AUTH_TIMEOUT)

    # 4. SMS 발송 (실습에서는 print로 대체)
    print(f"📧 [SMS 발송] To: {req_data.phone}, Code: {code}")

    # ⚠️ 실습용 — 운영 환경에서는 code를 응답에 포함하면 안 됩니다
    return {
        "message": "Verification code sent",
        "code": code,
        "expires_in": AUTH_TIMEOUT
    }

@app.post("/auth/verify")
async def verify_code(req_data: VerifyCodeRequest, request: Request):
    rd = request.app.state.redis

    hashed_phone = hashlib.sha256(req_data.phone.encode()).hexdigest()
    cache_key = f"auth:code:{hashed_phone}"

    # 1. Redis에서 저장된 인증번호 조회
    saved_code = await rd.get(cache_key)

    # [참고] Redis 6.2+ — GETDEL로 조회+삭제를 원자적으로 처리 가능
    # 단, 오타 입력 시 재발급이 필요한 트레이드오프가 있음
    # saved_code = await rd.getdel(cache_key)

    # 2. 코드가 없으면 만료되었거나 발송된 적 없는 것
    if not saved_code:
        raise HTTPException(status_code=400, detail="Code expired or not requested")

    # 3. 입력값 비교
    if saved_code != req_data.input_code:
        raise HTTPException(status_code=400, detail="Invalid code")

    # 4. 인증 성공 시 즉시 삭제 (재사용 방지)
    await rd.delete(cache_key)

    return {"message": "Authentication successful"}
```

---

## 서버 실행

```bash
python -m uvicorn main:app --reload
```

---

## 테스트 순서

### ① 인증번호 발송

```
POST http://localhost:8000/auth/send
Body: {"phone": "01012345678"}
```

### ② Redis CLI에서 확인

```bash
docker exec -it my-redis redis-cli

127.0.0.1:6379> KEYS auth:code:*
# 1) "auth:code:a3f2c1..."

127.0.0.1:6379> GET auth:code:a3f2c1...
# "483921"

127.0.0.1:6379> TTL auth:code:a3f2c1...
# 287
```

### ③ 인증번호 검증 (성공)

```
POST http://localhost:8000/auth/verify
Body: {"phone": "01012345678", "input_code": "483921"}
```

### ④ 재사용 시도 (실패 확인)

```
POST http://localhost:8000/auth/verify
Body: {"phone": "01012345678", "input_code": "483921"}
# {"detail": "Code expired or not requested"}
```

---

## 보안 강화 — secrets 모듈 사용

```python
import secrets

# ❌ random 모듈 (암호학적으로 안전하지 않음)
code = str(random.randint(100000, 999999))

# ✅ secrets 모듈 (암호학적으로 안전한 난수)
code = str(secrets.randbelow(900000) + 100000)  # 100000 ~ 999999
```

---

## 전화번호 해싱 비교

```python
# ❌ 위험 — 전화번호 그대로 노출
cache_key = f"auth:code:01012345678"

# ✅ 안전 — SHA-256 해싱 후 사용
hashed_phone = hashlib.sha256(req_data.phone.encode()).hexdigest()
cache_key = f"auth:code:{hashed_phone}"
```
