# [패턴 1] Cache-Aside 전략

> 강의노트 Ch.06 — Redis 실무 활용 패턴 / [패턴 1] DB 부하를 줄이는 캐싱 (Cache-Aside) 전략

---

## 실습 코드

```python
import asyncio
import json
from contextlib import asynccontextmanager
from fastapi import FastAPI, Request, HTTPException
import redis.asyncio as redis

# 가상의 DB 데이터 (실제로는 MySQL, PostgreSQL 등에서 가져옵니다)
fake_db = {
    "user:1": {"name": "Kim", "email": "kim@example.com", "tier": "Gold"},
    "user:2": {"name": "Lee", "email": "lee@example.com", "tier": "Silver"}
}

@asynccontextmanager
async def lifespan(app: FastAPI):
    app.state.redis = redis.from_url("redis://localhost:6379/0", decode_responses=True)
    yield
    await app.state.redis.aclose()

app = FastAPI(lifespan=lifespan)

@app.get("/users/{user_id}")
async def get_user_profile(user_id: str, request: Request):
    rd = request.app.state.redis
    cache_key = f"user:profile:{user_id}"

    # 1. Redis에서 캐시 확인
    cached_user = await rd.get(cache_key)
    if cached_user:
        # 캐시 Hit
        print(f"🚀 Cache Hit! (user_id: {user_id})")
        return json.loads(cached_user)   # JSON 문자열 → dict

    # 2. 캐시 Miss 시 DB 조회
    print(f"🐌 Cache Miss! Fetching from DB... (user_id: {user_id})")

    user_data = fake_db.get(f"user:{user_id}")
    if not user_data:
        raise HTTPException(status_code=404, detail="User not found")

    # DB 조회 지연 시뮬레이션 (2초)
    await asyncio.sleep(2)

    # 3. Redis에 저장 (TTL 300초)
    await rd.set(cache_key, json.dumps(user_data), ex=300)  # dict → JSON 문자열

    return user_data
```

---

## 서버 실행

```bash
python -m uvicorn main:app --reload
```

---

## 테스트 순서

### ① 첫 번째 요청 — Cache Miss

```
GET http://localhost:8000/users/1
```

터미널 출력:
```
🐌 Cache Miss! Fetching from DB... (user_id: 1)
```

### ② 두 번째 요청 — Cache Hit

```
GET http://localhost:8000/users/1
```

터미널 출력:
```
🚀 Cache Hit! (user_id: 1)
```

### ③ Redis CLI에서 확인

```bash
docker exec -it my-redis redis-cli

127.0.0.1:6379> GET user:profile:1
# "{\"name\": \"Kim\", \"email\": \"kim@example.com\", \"tier\": \"Gold\"}"

127.0.0.1:6379> TTL user:profile:1
# 293
```
