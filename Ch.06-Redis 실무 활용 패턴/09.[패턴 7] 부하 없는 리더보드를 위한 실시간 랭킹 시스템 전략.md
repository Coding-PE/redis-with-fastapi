# [패턴 7] 실시간 랭킹 시스템 (Leaderboard)

> 강의노트 Ch.06 — Redis 실무 활용 패턴 / [패턴 7] 부하 없는 리더보드를 위한 실시간 랭킹 시스템 전략

---

## 실습 코드

```python
from contextlib import asynccontextmanager
from fastapi import FastAPI, Request, HTTPException
import redis.asyncio as redis
from datetime import date

@asynccontextmanager
async def lifespan(app: FastAPI):
    app.state.redis = redis.from_url("redis://localhost:6379/0", decode_responses=True)
    yield
    await app.state.redis.aclose()

app = FastAPI(lifespan=lifespan)

RANKING_KEY = f"leaderboard:daily:{date.today()}"

@app.post("/rank/score")
async def update_score(user_id: str, score_delta: float, request: Request):
    """유저의 점수를 실시간으로 업데이트합니다."""
    rd = request.app.state.redis

    # ZINCRBY: 기존 점수에 합산 (키나 멤버가 없으면 자동 생성)
    # score_delta가 음수면 점수 감소도 가능
    new_score = await rd.zincrby(RANKING_KEY, score_delta, user_id)

    return {"user_id": user_id, "current_score": new_score}

@app.get("/rank/top10")
async def get_top_rankers(request: Request):
    """상위 10명의 정보를 조회합니다."""
    rd = request.app.state.redis

    # desc=True: 높은 점수 순으로 조회 (내부적으로 ZRANGE ... REV 동작)
    top_list = await rd.zrange(RANKING_KEY, 0, 9, desc=True, withscores=True)

    result = [
        {"rank": i + 1, "user_id": m, "score": s}
        for i, (m, s) in enumerate(top_list)
    ]
    return {"top_rankers": result}

@app.get("/rank/around-me/{user_id}")
async def get_nearby_rank(user_id: str, request: Request):
    """특정 유저를 기준으로 앞뒤 2명을 포함한 '내 주변 5명'을 조회합니다."""
    rd = request.app.state.redis

    # 1. 내 현재 순위 확인 (0-indexed, 내림차순)
    my_rank = await rd.zrevrank(RANKING_KEY, user_id)

    if my_rank is None:
        raise HTTPException(status_code=404, detail="Ranking data not found")

    # 2. 내 주변 범위 계산 (앞 2명 ~ 뒤 2명)
    start = max(0, my_rank - 2)  # 0 미만 방지
    end = my_rank + 2

    # 3. 해당 범위 유저들 추출
    nearby_list = await rd.zrange(RANKING_KEY, start, end, desc=True, withscores=True)

    result = [
        {"rank": start + i + 1, "user_id": m, "score": s}
        for i, (m, s) in enumerate(nearby_list)
    ]
    return {"user_id": user_id, "nearby_rankers": result}
```

---

## 서버 실행

```bash
python -m uvicorn main:app --reload
```

---

## 테스트 순서

### ① 점수 등록

```
POST http://localhost:8000/rank/score?user_id=user_a&score_delta=300
POST http://localhost:8000/rank/score?user_id=user_b&score_delta=150
POST http://localhost:8000/rank/score?user_id=user_c&score_delta=450
POST http://localhost:8000/rank/score?user_id=user_d&score_delta=200
POST http://localhost:8000/rank/score?user_id=user_a&score_delta=100   # 합산 → 400
```

### ② 상위 10명 조회

```
GET http://localhost:8000/rank/top10
```

### ③ 내 주변 순위 조회

```
GET http://localhost:8000/rank/around-me/user_d
```

### ④ Redis CLI에서 확인

```bash
docker exec -it my-redis redis-cli

127.0.0.1:6379> ZRANGE leaderboard:daily:2026-06-20 0 9 REV WITHSCORES
# 1) "user_c"
# 2) "450"
# 3) "user_a"
# 4) "400"
# 5) "user_d"
# 6) "200"
# 7) "user_b"
# 8) "150"

127.0.0.1:6379> ZREVRANK leaderboard:daily:2026-06-20 user_d
# (integer) 2   ← 0-indexed, 사용자에게는 +1 하여 3위로 표시
```
